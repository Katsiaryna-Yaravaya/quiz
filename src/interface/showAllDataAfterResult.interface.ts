import { AllSavedAnswer } from './index.interface'

export interface ShowAllDataAfterResult {
  chooseByUser: string[]
  allSavedAnswer: AllSavedAnswer[]
  correctAnswer: string[]
  nameQuestion: string[]
}
